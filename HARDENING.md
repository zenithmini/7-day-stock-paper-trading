# 安全修補紀錄

審查基準：yomislight/7-day-stock-paper-trading，commit `332d3c481855209d4a28bdeab92965b3bbeb4e29`。
日期：2026-09-20。範圍：先修補既有研究工具，不啟用帳戶交易或背景排程。

| 缺口 | 修改 | 驗證 |
| --- | --- | --- |
| 高權限密鑰仍可通過 | 讀取 apiRestrictions 後才繼續帳戶／股票讀取；交易、提領、劃轉等欄位必須明確為 false；帳本再次檢查 | 錯誤型別、缺少欄位、各權限為 true、未知能力旗標及 API 呼叫順序 |
| run_id 可跳出輸出目錄 | 字元與長度限制、Windows 保留名稱、輸出路徑及符號連結檢查 | 絕對路徑、../、反斜線、換行、超長參數、符號連結 |
| 模擬帳本不同程序互相覆蓋 | OS 跨程序鎖＋SQLite BEGIN IMMEDIATE；在每次操作開始重新載入狀態 | 舊實例覆寫、跨程序鎖競爭、重複 run_id |
| 寫入中斷造成帳本不一致 | SQLite 同一交易保存資金及事件；JSON 為匯出，reconcile 可重建 | 提交後匯出故障、重播、失敗平倉回滾 |
| 個人資料隨 Git／雲端排程外傳 | 新工作區完全隔離於 .local；CI 無帳戶讀取、無 Secrets、無紀錄上傳及 Telegram | 工作流程回歸檢查、初始化不覆蓋資料 |
| Actions 每次從舊狀態開始 | 移除雲端帳戶執行；持續狀態只在本機工作區 | CI 僅執行離線測試，不再承擔帳本持久化 |
| 原作者的驗證狀態被繼承 | 初始化帳戶未驗證、日期空白、模擬交易及排程停用 | 初始化回歸測試 |
| 報價請求耗時被當作零 | 接收後取時間、5 秒請求上限、明確未驗證來源事件時間 | 模擬 6 秒報價延遲必須拒絕 |
| 模型／排程能力敘述超出實作 | 首頁明確列出仍缺少 AI、24 小時監控及持續止損 | 人工文件核對 |

## 資料完整性

第一次建立本機帳本時從初始化 JSON 建立 SQLite；之後 JSON 不再是帳本輸入。
禁止兩個程序交錯寫入。資料庫提交後，即使 JSON 匯出失敗，交易仍可能已完成；應先 reconcile，
再以相同 run_id 重試，不得把「匯出失敗」誤認為「模擬交易沒有發生」。

觀察排程仍使用自己的 SQLite 執行日誌，持倉及資金由模擬帳本資料庫獨立管理。
人工 research 文件不是交易授權，也不能覆寫帳本數字。
備份使用 SQLite backup API，不直接複製可能仍有 WAL 的資料庫主檔。

## 安全邊界

密鑰的秘密仍只用於本機 HMAC，只有 Key 與簽章送至官方 Binance。
讀取密鑰權限本身需要一次已簽名請求；程式無法在發出該請求之前知道密鑰權限。
權限介面有新增、缺少或型別變更時採拒絕策略，可能需要更新相容性，而不是放寬權限。
支援的只讀 FIX 權限不視為交易權限。權限欄位參考：
[Binance 官方 API Key Permission](https://developers.binance.com/en/docs/catalog/core-trading-wallet/api/rest-api/account#get-api-key-permission-user_data)。

本程式不是作業系統沙箱：可控制本機使用者帳戶、修改 Python 原始碼或資料庫的人仍可改變模擬結果。
路徑與鎖限制用來防止不良輸入和意外並行，不宣稱抵抗已取得同一 OS 帳戶權限的惡意程序。

## 驗證與尚未驗證

執行 `python -B run_tests.py`，真實 socket 連線被阻擋；測試使用虛構資料與暫存目錄。
新增故障與安全邊界測試，並更新既有測試以符合權限與執行紀錄分離的新契約。
目前工作環境驗證為 Linux / Python 3.12；Windows/macOS 的結果以 GitHub CI 為準。
未使用真實密鑰、未讀取使用者帳戶、未發出真實訂單、未驗證獲利、未測試 24 小時運行。
未完整審計第三方 GitHub Actions 的實作；採官方來源並固定 commit SHA，checkout 不保存寫入憑證。

## 保留限制

本版本仍是股票／ETF 研究工具，沒有加密貨幣行情服務、AI 推論、策略自動學習或 GUI。
定期檢查不等於連續止損；網路失敗會結束本次工作。管理命令被呼叫時才處理不隔夜與止損。
API 接收時間不等於交易所報價產生時間，故不宣称市場事件時間已經過驗證。
舊資料保留在 Git 歷史及原目錄作為上游參考，新執行程式不將其視為使用者的現況。
